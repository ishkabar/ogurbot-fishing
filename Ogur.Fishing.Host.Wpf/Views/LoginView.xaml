<UserControl x:Class="Bot.Host.Wpf.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="24">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="420">
            <TextBlock Text="Sign in to OgurBot" FontSize="22" FontWeight="Bold" Margin="0,0,0,12" />
            <TextBox Margin="0,6,0,6" Height="34" PlaceholderText="Email or username"/>
            <PasswordBox Margin="0,6,0,12" Height="34"/>
            <Button Content="Continue" Height="38" Command="{Binding ContinueCommand}"/>
            <TextBlock Text="Auth is stubbed for now." Opacity="0.6" Margin="0,12,0,0" />
        </StackPanel>
    </Grid>
</UserControl>