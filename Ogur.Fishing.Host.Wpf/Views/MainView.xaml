<UserControl x:Class="Bot.Host.Wpf.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
            <Button Content="Start Fishing" Command="{Binding StartCommand}" Width="130" Height="34" Margin="0,0,8,0"/>
            <Button Content="Stop" Command="{Binding StopCommand}" Width="90" Height="34"/>
            <TextBlock Text="Status:" Margin="16,0,4,0" VerticalAlignment="Center"/>
            <TextBlock Text="{Binding Status}" FontWeight="Bold" VerticalAlignment="Center"/>
        </StackPanel>
        <ListBox Grid.Row="1" ItemsSource="{Binding Events}"/>
    </Grid>
</UserControl>